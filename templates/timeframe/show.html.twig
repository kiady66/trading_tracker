{% extends 'base.html.twig' %}

{% block title %}Timeframe{% endblock %}

{% block body %}
    <h1>Timeframe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ timeframe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ timeframe.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_timeframe_index') }}">back to list</a>

    <a href="{{ path('app_timeframe_edit', {'id': timeframe.id}) }}">edit</a>

    {{ include('timeframe/_delete_form.html.twig') }}
{% endblock %}
