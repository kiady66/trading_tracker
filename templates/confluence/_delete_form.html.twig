<form method="post" action="{{ path('app_confluence_delete', {'id': confluence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confluence.id) }}">
    <button class="btn">Delete</button>
</form>
