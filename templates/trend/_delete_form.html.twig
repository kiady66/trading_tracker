<form method="post" action="{{ path('app_trend_delete', {'id': trend.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trend.id) }}">
    <button class="btn">Delete</button>
</form>
