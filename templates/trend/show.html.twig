{% extends 'base.html.twig' %}

{% block title %}Trend{% endblock %}

{% block body %}
    <h1>Trend</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trend.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trend.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_trend_index') }}">back to list</a>

    <a href="{{ path('app_trend_edit', {'id': trend.id}) }}">edit</a>

    {{ include('trend/_delete_form.html.twig') }}
{% endblock %}
