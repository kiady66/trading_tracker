{% extends 'base.html.twig' %}

{% block title %}TradeType{% endblock %}

{% block body %}
    <h1>TradeType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trade_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trade_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_trade_type_index') }}">back to list</a>

    <a href="{{ path('app_trade_type_edit', {'id': trade_type.id}) }}">edit</a>

    {{ include('trade_type/_delete_form.html.twig') }}
{% endblock %}
