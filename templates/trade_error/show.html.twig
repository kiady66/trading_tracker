{% extends 'base.html.twig' %}

{% block title %}TradeError{% endblock %}

{% block body %}
    <h1>TradeError</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trade_error.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trade_error.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_trade_error_index') }}">back to list</a>

    <a href="{{ path('app_trade_error_edit', {'id': trade_error.id}) }}">edit</a>

    {{ include('trade_error/_delete_form.html.twig') }}
{% endblock %}
