
###> symfony/framework-bundle ###
# APP_SECRET : à définir dans .env ou .env.local (jamais ici — fichier commité,
# repo public). Attention à l'ordre de chargement : .env.dev écrase .env.local,
# donc pas de ligne APP_SECRET= vide ici.
###< symfony/framework-bundle ###
